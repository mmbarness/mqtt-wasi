[package]
name = "tls-pubsub"
version = "0.1.0"
edition = "2021"

[dependencies]
mqtt-wasi = { path = "../..", features = ["tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
opt-level = "z"
strip = true
panic = "abort"
