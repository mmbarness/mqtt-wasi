[package]
name = "tls-pubsub"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
mqtt-wasi = { path = "../..", features = ["tls"] }

[profile.release]
lto = true
opt-level = "z"
strip = true
panic = "abort"
